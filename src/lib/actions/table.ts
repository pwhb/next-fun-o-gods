export async function deleteAction(prevState: any, formData: FormData) 
{
    console.log("prevState", prevState);
    return {};
    // await deleteMany(docs.map(doc => doc._id).filter((_, i) => selected[i]), collection);
};